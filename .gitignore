vendor/
*.test
*.out
_output/
*.tfstate
*.tfstate.backup
.idea/
.vscode/
.env
# Built binary at the repo root, NOT cmd/provider/ (the source dir).
# Without the leading slash this pattern matched cmd/provider/ too.
/provider
*.xpkg
